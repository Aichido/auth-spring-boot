# Application runtime
APP_PORT=8081

# Security keys (replace in real deployments)
MASTER_KEY=change-me-master-key-32-bytes-min
JWT_SECRET=change-me-jwt-secret-at-least-32-bytes

# CI/CD SonarCloud organization key (used in GitHub Actions repository variable)
SONAR_ORGANIZATION=your-sonarcloud-org-key
